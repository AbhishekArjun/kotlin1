fun main() {
    val numbers = listOf(1, 2, 3, 4, 5)
    for (number in numbers) {
        val result = when {
            number % 2 == 0 -> "$number is Even"
            else -> "$number is Odd"
        }
        println(result)
    }
}
